### File Overview

Thin client page for `/leads/[leadId]`. Guards with `useAuthorize("LEADS")`, reads `tab` search param, renders `LeadDetailsScreen`.

### Navigation

Locale-prefixed `/en/leads/{uuid}?tab=conversation` (etc.).
