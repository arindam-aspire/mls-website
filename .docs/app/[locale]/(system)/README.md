# Route group `(system)` — `app/[locale]/(system)/`

System-level locale routes (errors, access denial). Group name does **not** appear in the URL.

## Routes

| Path | File |
| --- | --- |
| `/en/unauthorized` | `(system)/unauthorized/page.tsx` |

## Layout

[layout.md](./layout.md) → `PublicLayout` for all routes in this group.
