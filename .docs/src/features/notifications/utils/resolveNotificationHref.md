# resolveNotificationHref

Resolves notification click targets. Supports saved-search deep links, lead notifications (`LEAD_*` / `lead_*` type keys → `/leads` or `/leads/{id}` with optional `tab=conversation|close`), property submission paths, and metadata `redirect_path` / `action_url` fallbacks.
