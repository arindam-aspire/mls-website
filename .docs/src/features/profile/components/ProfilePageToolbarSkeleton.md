# File Overview

Skeleton for `ProfilePageToolbar`: title, subtitle, and change-password button placeholders.

**Source:** `src/features/profile/components/ProfilePageToolbarSkeleton.tsx`

# Responsibilities

- Match responsive flex layout (`flex-col` → `sm:flex-row sm:justify-between`).
- Title block: two text skeleton lines; action: `rounded-lg` button-sized block.

# Exports

- `ProfilePageToolbarSkeleton`

# UI Details

- `Skeleton variant="text"` for headings; block skeleton `rounded-lg` for button (`h-9 sm:h-10`).

# Dependencies

- [ProfilePageToolbar.md](./ProfilePageToolbar.md)
