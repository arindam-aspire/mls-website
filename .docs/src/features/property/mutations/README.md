# Property mutations (`src/features/property/mutations/`)

React Query mutation hooks for property APIs that are triggered by explicit user actions.

| File | Export |
| --- | --- |
| [property.mutation.md](./property.mutation.md) | `useGetPropertyList` |

## Notes

- Uses `getPropertyList` from `services/property.service.ts`.
- Uses `useToast` for user-facing API error feedback.
