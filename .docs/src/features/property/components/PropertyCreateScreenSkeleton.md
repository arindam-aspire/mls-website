# File Overview

Loading skeleton for `PropertyCreateScreen` while property taxonomy, location taxonomy, and feature catalog APIs resolve.

**Source:** `src/features/property/components/PropertyCreateScreenSkeleton.tsx`

# Responsibilities

- Mirror create screen layout: header row (title, subtitle, breadcrumb placeholders on `md+`) and centered card block.
- Use shared `Skeleton` primitives (`text`, `block`).

# Dependencies

- [PropertyCreateScreen.md](../screens/PropertyCreateScreen.md)
