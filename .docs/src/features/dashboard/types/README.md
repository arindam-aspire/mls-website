# Dashboard types

TypeScript contracts shared by the dashboard service, hook, and components.

The module models the API response without `any`, including fixed activity tones and health-alert severities so UI visual mappings remain exhaustive.
