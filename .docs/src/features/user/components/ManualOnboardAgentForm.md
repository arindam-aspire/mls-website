# `ManualOnboardAgentForm`

**Source:** `src/features/user/components/ManualOnboardAgentForm.tsx`

Manual onboarding fields: full name, email, phone (`PhoneInput`), and service area (`MultiSelectDropdown` from location taxonomy).

## Consumers

- `ManualOnboardAgentModal`
