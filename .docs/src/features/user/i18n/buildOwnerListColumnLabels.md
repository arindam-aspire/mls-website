# `buildOwnerListColumnLabels`

**Source:** `src/features/user/i18n/buildOwnerListColumnLabels.ts`

Maps `user.owners.list.columns` keys to labels for the owners table and column picker:

`owner`, `phone`, `email`, `properties`, `leads`, `status`, `actions`.
