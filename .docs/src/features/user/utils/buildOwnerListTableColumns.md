# `buildOwnerListTableColumns`

**Source:** `src/features/user/utils/buildOwnerListTableColumns.ts`

Builds localized `TableColumn<Owner>[]` via `buildOwnerTableColumns`, applies column visibility, and resolves pinned/grid-hidden column ids for `OwnerListView`.
