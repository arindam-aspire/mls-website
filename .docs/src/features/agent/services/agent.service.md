# `getAgentList`

**Source:** `src/features/agent/services/agent.service.ts`

`GET /agents` with auth. Returns `{ agents, pagination }`.

Default query: `page=1`, `pageSize=10`, `sortBy=invited_at`, `sortOrder=desc`.
