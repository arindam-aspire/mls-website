# leadList.types

### File Overview

Row shape for the leads list table (`AgentListView`).

### Exports

`LeadListRow` — id, lead number, property/customer labels, status, assigned agent, created date display + sort value.
