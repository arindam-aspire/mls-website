# API clients (`src/apis/clients/`)

| File | Exports |
| --- | --- |
| [api.client.md](./api.client.md) | `BaseApiClient`, `authClient`, `apiClient`, `ApiRequestConfig` |

`authClient` / `apiClient` differ only in default `auth` flag passed to interceptors.
