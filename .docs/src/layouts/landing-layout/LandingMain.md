# File Overview

Landing main content wrapper for landing routes.

**Source:** `src/layouts/landing-layout/LandingMain.tsx`

# Responsibilities

- Render the main content area for landing routes.
- Apply landing-specific top offset and edge-to-edge content when pathname is `/`.

# Imports

- `usePathname` from `src/i18n/navigation`
- `cn` from `src/lib/cn`

# Exports

- `LandingMain`

# Notes

- This file is now fully implemented in `landing-layout` (no re-export).
