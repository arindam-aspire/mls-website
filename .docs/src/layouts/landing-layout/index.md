# File Overview

Landing layout entry component.

**Source:** `src/layouts/landing-layout/index.tsx`

# Responsibilities

- Provide a dedicated landing layout import path.
- Compose and return layout shell for landing routes.

# Imports

- `AuthModal` from `src/features/auth/components/AuthModal`
- `LandingHeader`, `LandingMain`, `LandingFooter`, `LandingBottomTabBar` from `src/layouts/landing-layout`

# Exports

- `LandingLayout`
- `default`

# Notes

- Uses landing-prefixed modules so naming and entry points stay scoped to the landing layout.
