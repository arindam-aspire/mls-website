# File Overview

Landing header theme toggle module.

**Source:** `src/layouts/landing-layout/LandingHeaderThemeButton.tsx`

# Responsibilities

- Render the landing header light/dark toggle button.
- Apply hero-aware styling while toggling theme via `ThemeProvider`.

# Imports

- `IconButton` from `src/components/ui/icon-button`
- `useTheme` from `src/providers/ThemeProvider`
- `Moon` and `Sun` icons from `lucide-react`

# Exports

- `LandingHeaderThemeButton`

# Notes

- This file is now fully implemented in `landing-layout` (no re-export).
