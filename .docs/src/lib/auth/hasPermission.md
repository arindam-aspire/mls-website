# File Overview

Checks whether a logged-in user has a permission key from `PERMISSIONS`.

**Source:** `src/lib/auth/hasPermission.ts`

# Exports

- `hasPermission(user, permission)` → `boolean`

# Dependencies

- [permissions.md](./permissions.md), `LoggedInUser` type
